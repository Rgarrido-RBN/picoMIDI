#include <stdio.h>
#include "pico/stdlib.h"
#include "gpioPico.h"
#include "gpioDeclaration.h"

int main() 
{
    // TODO: Initialize gpio pins, after init the you have to enable the interrupt
    // functionality calling to gpioPico::gpioEnableInterrupt(uint pinID, uint32_t event, bool enableFlag)
return 0;
}
